```
         <start> := (REWRITE <expression>+)
                  | (FETCH <ATOMIC>)
    
    <expression> := (RULE (VAR <ATOMIC>+)? (READ (EXP <ANY>)) (WRITE (EXP <ANY>)))
                  | <start>
```
```
(CONSL <ANY>) -> <RESULT>
(CONSA <ANY>) -> <RESULT>
```
